<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student.androidlistactivitys">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ComputerActivity"></activity>
        <activity android:name=".ComputerAddActivity" />
        <activity android:name=".PhoneActivity" />
        <activity android:name=".SqlightListActivity" />
        <activity android:name=".NotesActivity" />
        <activity android:name=".NotepadActivity" />
        <activity android:name=".ImageMatrixActivity" />
        <activity android:name=".ShapeViewActivity" />
        <activity android:name=".WebActivity" />
        <activity android:name=".MatrixActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".Main4Activity" />
        <activity android:name=".Main5Activity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".ArrayCalcActivity" />
        <activity android:name=".MathActivity" />
        <activity android:name=".ArrayActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".CyclesActivity" />
        <activity android:name=".RequaestActivity" />
        <activity android:name=".ResycleListActivity" />
        <activity android:name=".DeviceAddActivity" />
        <activity android:name=".PhoneAdd" />
        <activity android:name=".ComputerAdd" />
    </application>

</manifest>